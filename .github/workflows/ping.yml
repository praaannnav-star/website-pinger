name: Ping Website

on:
  schedule:
    - cron: '*/13 * * * *'

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Visit Website
        run: |
          curl -L "https://nayepankh-backend-jvx9.onrender.com"
